# jaysqvl portfolio app

Next.js app for `jaysqvl.com`.

```bash
npm install
npm run dev
```

Useful checks:

```bash
npm run lint
npm run build
```

The production site deploys from `main`; redesign work is reviewed from the `redesign/homelab-neutral` branch through Vercel previews.
